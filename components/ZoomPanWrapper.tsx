/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/

// This component is a placeholder.
// The zoom and pan logic has been integrated into the `useZoomPan` hook 
// and applied directly to the wrapper div in `App.tsx` for simplicity and better state coherence.
import React from 'react';

const ZoomPanWrapper: React.FC = () => {
  return null;
};

export default ZoomPanWrapper;
